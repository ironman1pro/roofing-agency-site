name = "roofing-agency-site"
main = "src/worker.js"
compatibility_date = "2026-09-01"

[assets]
directory = "./public"
binding = "ASSETS"
not_found_handling = "404-page"
# Only /api/* runs the Worker; every other request is served straight from ./public
run_worker_first = ["/api/*"]
